# Dependencies & build artifacts
node_modules/
dist/
coverage/
*.tsbuildinfo
__pycache__/
*.pyc
.DS_Store
backend/.aws-sam/
backend/build/
frontend/dist/

# Secrets & local env — NEVER commit. Use the *.example templates instead.
.env
.env.*
!.env.example
!.env.deploy.example
!.env.*.example
.env.deploy.local
.local-resource-names.env
*.bak
*.bak.*
*.pem
*-key.json
service-account*.json

# Scratch / local-only notes & data (real candidate PII, analysis outputs — never commit)
junk/
local-notes/
.claude/

# contest-eval monitoring: live/PII output (alerts carry candidate usernames + code)
monitoring/.data/
monitoring/**/data/raw/
monitoring/**/data/processed/
monitoring/**/code_*.json
monitoring/**/contest_*_meta.json
