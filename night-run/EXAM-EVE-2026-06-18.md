# Proctor Exam-Eve Plan — live test 2026-06-18 (~700 students)

> **Canonical source of truth for this overnight run.** Referenced by the session `/goal`.
> If the session compacts or resumes, re-read this file first — it is self-contained.
> Created 2026-06-17 (Karthi's place). Output mode: CLI. Project: `aerele-proctor-dev`.

## Stakes & non-negotiables (apply to EVERY step)

- ~700 students take a live coding test on **2026-06-18**. **Nothing may break.**
- **No-break gate at every step:** build/tests green **and** a concrete no-break check before moving on. Never claim done without evidence.
- **Clean base first:** resolve PR #2 to ONE canonical `master` **before committing any code**. Then cut a fresh branch off it. (Building fixes on the still-diverged branch would entangle the git surgery with exam-critical code.)
- **PII gate:** run `/home/karthi/arogara/pii-audit/scan.sh /home/karthi/arogara/proctor` and get working-tree + history clean **before ANY push**.
- **Deploy:** gcloud at `~/google-cloud-sdk/bin/gcloud`; deploy with `--update-env-vars` ONLY (never `--set-env-vars` — it wipes Judge0/invigilator/admin/sweep env). Set `min-instances=1` on api + web for the exam window.
- **Commits:** serialized (no concurrent `git commit`); trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- **Wording rule:** do NOT surface the raw interaction-capture term in the admin panel, any user-facing text, or commit messages. Use neutral framing — "based on detailed test data" / "detailed analysis"; "interaction analytics" is tolerable but never highlighted. *(This file follows that rule.)*
- **Morning-discussion list:** record judgment calls below instead of silently guessing.

## Sequence (do in this order)

0. **[DECIDED 2026-06-17 — Option B, no force-push]** reconcile via `git merge -s ours origin/master`, then merge PR #2. See PR #2 below.
1. **#69** Land PR #2 → one clean canonical `master`. Run PII scan before pushing. Verify origin/master = full exam-platform code, PII-clean.
2. **#77** Clean up / organize `night-run/` (keep THIS file as the anchor; archive old runs, remove cruft, conservative). Cut fresh branch `fix/exam-eve-2026-06-18` off the clean master.
3. **Exam-critical fixes** (each: spec → TDD → verify → commit → no-break check):
   - **#72** No-roster scoring correctness — **HIGHEST**. Tomorrow no roster is uploaded; students self-enter all details. Verify session start, identity capture, and that scores display correctly with ZERO enrollment spine (not just roster-then-deleted). Fix if any path assumes a roster exists.
   - **#70** Kill native browser dialogs that exit fullscreen. Stub-reload currently fires a native `confirm`/`beforeunload` → boots the student out of fullscreen → they must re-confirm + re-enter + retype. Audit every native dialog in the student flow; after entering fullscreen there must be ZERO native confirmations (replace with in-UI modals). Also find any other accidental fullscreen-exit paths.
   - **#71** Admin live toggle: fullscreen red-screen "no-typing" recovery. **Correction from Karthi:** the toggle does NOT remove the whole red screen — it removes only the **typing step**. When on: keep a warning message + the "Enter full screen again" button as the only required action (no typing). Control lives on the admin contest/test page and must take effect **live during a running exam** (propagate to in-progress sessions).
   - **#73** On-demand "Run evaluation" button on the admin results page. Determine current eval trigger (every submission / interval / on-view) and make it button-triggered: compute scorecards + integrity/talent verdicts + tags on demand, any time, idempotent/re-runnable.
4. **Analysis & polish:**
   - **#74** Mine the recorded test-data backup from the last test for reliable cheating-vs-genuine signals; surface the reliable ones in the admin results panel (neutral wording per the rule). Karthi reviews the patterns.
   - **#75** Scrub the raw capture term from admin UI strings + commit messages; use neutral framing.
5. **#76** Finale gate: triple review (code-review on the diff + independent correctness lens + security/PII pass) → deploy to `aerele-proctor-dev` (`--update-env-vars` only) → run smoke tests → `min-instances=1`.

## PR #2 — diagnosis & landing

**Root cause (not a second author — Karthi was the only pusher):** the PII history-rewrite done before the first push gave every commit a new SHA from the redaction point onward. So the PR branch `feat/exam-platform-2026-06-13` (tip `4f35ab5`, 299 commits) and `origin/master` (tip `9cc4970`, 2026-06-08, 31 commits) diverged in SHA even though the content lines up — GitHub can't fast-forward and flags conflicts.

**Verified safe & lossless (read-only forensics, 2026-06-17):**
- Branch is a strict content **superset** of `origin/master`: all 31 origin commit subjects present in the branch; origin's tip is byte-identical to its rewritten twin (`74aff0aa`), which is an ancestor of the branch tip.
- `origin/master` is **PII/secret-clean**: 0 commits with the verdict-queue PII path; 0 commits containing the API key (any file, full history); the key file `RESUME-ANCHOR.md` doesn't even exist there.
- Only files on origin/master but not the branch tip: 7 internal `night-run/` scratch docs (GOAL.md, HOW-TO-RUN.md, MORNING-REVIEW.md, NIGHT-LOG.md, PR-BODY.md, RESUME-STATE.md, RESUME.md) — no code/functionality.

**Landing options (the only force-push-vs-not call in this run):**
- **A. Clean linear (force-push):** replace `master` with the rewritten clean history. Single tidy lineage, no duplicate commits. It's a force-push to public master; mitigations: sole user, old tip 9 days old + verified clean + fully contained, recoverable from `junk/pre-pii-rewrite-backup.bundle` + reflog.
- **B. No force-push (merge):** reconcile via `git merge -s ours origin/master` on the branch, then merge PR #2. Honors the no-force-push rule, but master keeps the old pre-rewrite lineage reachable → ~31 commit subjects appear duplicated in history. Functionally identical tree.

Backup of pre-rewrite state: `junk/pre-pii-rewrite-backup.bundle`.

## Stretch goal — resume the architecture-decomposition refactor

Karthi (2026-06-17): we were mid-refactor (the god-file decomposition). Base refactor B0+B1 is done + cut over, then we stopped. RESUME the remaining phases following the EXISTING plan, with the hard rule: **at every step everything must stay working + fully wired up** (behavior-preserving; tests green; no dangling/dead wiring).

STRICT SEQUENCING (Karthi's order — do NOT start the stretch until all of this holds):
1. Finish ALL current goals (#70–#77).
2. MERGE them to master.
3. Deploy to Google Cloud **ONCE** (after all current goals, before the stretch).
4. THEN start the stretch on a **SEPARATE branch** (e.g. `refactor/decomp-resume`).
5. Morning: review together; if good, merge the stretch too.

LOCATED (2026-06-18): plan = `docs/superpowers/plans/2026-06-11-architecture-decomposition.md`. B0 DONE (commits e44ff44→0dc1ae6: leaf extraction to lib/, env→config.mjs, GCP clients→lib/clients.mjs, makeAuth/makeSessionStore factories, recursive scoping/env lint). B1 DONE (ff88c91: routes/invigilator.mjs factory + routesAuthLint). PAUSED at f40a7e5 ("PAUSE after B0/B1 green — post-test"). RESUME = **B2+** (per 237c9bc). Execute phase-by-phase with the every-step-works rule + full test runs.

## Progress log (cont. 3)

- **2026-06-18 (TRIPLE REVIEW done — overall GO):** 3 lenses (code / security-PII / exam-readiness) on master..HEAD → 2× SHIP-WITH-FIXES, overall GO-to-deploy (nothing exam-fatal). Fixing 2 MAJORs + 1 MINOR before the single deploy (workstream B3): (1) eval scan caps were DEAD CODE — raise the real handler.mjs constants SESSIONS_QUERY_LIMIT 2000→6000 + SUBMISSIONS_RESULTS_LIMIT 50000→120000 (override shadowed the evaluation.mjs defaults); (2) mid-exam live-flip of simplified-recovery could STRAND a student already in fullscreen (no tryResolve on the flip) → mirror the exemptFullscreen config_change release; (3) a11y — focus the re-enter button in simplified mode. Security/PII: clean (gitleaks 0, flag can't bypass proctoring, fixtures+screenshots synthetic) EXCEPT the pre-existing roll-in-history (→ morning list). Deferred NITs + ops notes → morning list.

**Karthi refinement (2026-06-18):** BEFORE executing the stretch — (1) re-read the decomposition plan COMPLETELY; (2) reconcile it against the CURRENT code, flagging any drift/changes since the plan was written; (3) UPDATE/improve the plan for the current structure; (4) only once satisfied with the plan in its current state, break it into steps and complete them one-by-one — with everything working and the product USABLE at EACH step.

## Remaining build specs (after A commits) — sequence B1 → B2 → #77 (shared files: ResultsPanel.tsx, handler.mjs, api.ts → do NOT parallelize)

**B1 — no-roster (#72):** (a) neutralize the alarming "N submitters not on the roster" banner (ResultsPanel.tsx ~375-382) + per-row "unmatched identity" badge (~517) when the contest has no roster → neutral "self-entered" framing (add a `no_roster` flag from computeContestResults, handler.mjs ~3995-4063 / scoreboard.mjs buildResultsRows). (b) VERIFY duplicate-login is graceful for no-roster: same (roll, contest) = same student re-login → 2nd waits/RESUMES cleanly (not a dead-end pending_approval), scores attributed to one identity (handler.mjs acquireLiveSlot ~1109 + /session/resume + identity.mjs); polish UX if confusing. (c) add a test for the zero-enrollment Results path (configured:true, rows scored, neutral banner). (d) NO in-panel selection build (Karthi: OK to lose it). Uniqueness key = roll + contest = username_norm within contest scope.

**B2 — eval + wording (#73/#74/#75):** (a) rename the existing "Evaluate contest" button → "Run evaluation" + expose a visible **force re-run** control (force param already wired api.ts:1807→evaluation.mjs:304); ResultsPanel.tsx ~459-467 + onEvaluate ~252. (b) RAISE `sessionsQueryLimit` (2000 → ≥5000) + verify `submissionsQueryLimit` (50000) in evaluation.mjs — 700 students × resumes may exceed 2000 session docs and under-sample the cross-pass. (c) #74 polish: grammar "a exact"/"a skeleton" → "an exact"/"a skeleton" (evaluationMetrics.mjs:1448/1456). (d) #75 wording: reword evaluationMetrics.mjs:548 ("Run of N keystrokes…") + :556 ("Keystroke inter-key gaps…") → neutral ("typed characters" / "inter-character timing"); update the 3 test assertions (evaluationMetrics.test.mjs:31/153/163). Future commits avoid the term. LEAVE student consent strings (truthful PII disclosure — morning call) + past commit messages (no history rewrite — morning call). Ship `projectEvaluation` surface UNCHANGED (validated against ground-truth: 0 false positives).

## Progress log (cont. 2)

- **2026-06-17 (A COMMITTED):** Workstream A fullscreen #70+#71 → commit `c0c5866` (16 files, +393/−64). Review verdict SHIP; polish applied (single-action recovery copy when simplified flag on + modal Esc/autofocus + stale-comment cleanup). My independent final gate: backend 858/0, frontend 772/772, tsc clean. Security invariant test-pinned (flag never drops the fullscreen requirement). #70+#71 DONE. Dispatched B1 (no-roster #72).
- **2026-06-18 (B1 COMMITTED):** no-roster #72 → commit `4486772` (5 files, +220/−8). `no_roster` signal (no active enrollment AND no roster meta) → neutral "self-entered" Results banner + badge; rostered contests unchanged. Duplicate-login VERIFIED graceful (roll+contest = one identity; same-device reconnect RESUMES; concurrent 2nd-device login → pending_approval admin-approve; stale-lock auto-recovers; no merge/split) — NO live session-path code changed. Tests: zero-enrollment Results path + no-roster duplicate-login/resume. Gate: backend 862/0, frontend 772, tsc clean. #72 DONE. Dispatched B2 (#73 eval rename + force re-run + sessionsQueryLimit bump for 700; #74 grammar; #75 admin wording).
- **2026-06-18 (B2 COMMITTED):** eval+wording → commit `ba4e302` (3 files, +37/−12). "Run evaluation" + confirm-gated "Force re-run"; sessionsQueryLimit 2000→6000 + submissionsQueryLimit 50000→120000 for 700; clone-cluster grammar fix; admin cadence-evidence reworded to neutral (no raw term in admin panel). Gate: backend 862/0, frontend 772, tsc clean. #73/#74/#75 DONE. ALL feature workstreams (A,B1,B2) committed on `fix/exam-eve-2026-06-18`. Next: #77 night-run cleanup (+ PII recon of tracked night-run/), then #76 triple-review → merge to master → deploy once → min-instances=1.

## Browser / E2E tooling (verified 2026-06-17)

Headless Chromium on :9222 works + the chrome-devtools MCP connects (no Karthi setup needed). Relaunch if needed:
`setsid chromium --remote-debugging-port=9222 --user-data-dir=/tmp/proctor-e2e-profile --no-first-run --no-default-browser-check --no-sandbox --headless=new --use-fake-ui-for-media-stream --use-fake-device-for-media-stream about:blank >/tmp/chromium-e2e.log 2>&1 & disown`
E2E target = deployed web (Cloud Run) or a local dev server. Admin password in `.env.deploy.local` (never print). Fullscreen-exit fidelity may be limited headless → lean on unit tests there.

**Deploy baseline (verified 2026-06-17):** `proctor-deployer` SA can read Cloud Run. Current live revs: `proctor-api-00017-jng` · `proctor-web-00018-nfl`, **min-instances=0** on both → SET =1 at the #76 deploy for the exam. URLs: api `https://proctor-api-ej4cpz43iq-el.a.run.app` · web `https://proctor-web-ej4cpz43iq-el.a.run.app`. **SAFE DEPLOY METHOD (env-preserving — do NOT run `deploy-gcp.sh` as-is; its `--set-env-vars` WIPES the 6 extra vars).** `proctor-api` currently carries 14 env vars: EVIDENCE_BUCKET, ADMIN_PASSWORD, ALERTS_INGEST_API_KEY, ALERTS_COLLECTION, PUBLIC_APP_ORIGIN, SESSION_COLLECTION, SETTINGS_COLLECTION, URL_EXPIRY_SECONDS, JUDGE0_MODE, JUDGE0_BASE_URL, JUDGE0_RAPIDAPI_HOST, **JUDGE0_API_KEY, INVIGILATOR_PASSWORD, RETENTION_SWEEP_API_KEY**. `proctor-web` has NO env (static). Deploy by IMAGE-SWAP (Cloud Run carries forward all unspecified config incl. env when you pass only --image): (1) `gcloud builds submit backend --tag <REGION-docker.pkg.dev/PROJECT/REPOSITORY/api:latest>` (read REPOSITORY from deploy-gcp.sh); (2) `gcloud run deploy proctor-api --image <img> --region asia-south1 --min-instances=1` with NO env flags → swaps code, preserves all 14 vars, sets min-instances=1. Same for proctor-web (build + deploy --image --min-instances=1). Then smoke: web 200; api admin routes 401 unauth. NOTE: a redeploy re-applies `gcs-lifecycle.json` (re-arms the KPR-evidence age-3 delete) — KPR data already copied to `aerele-proctor-dev-kpr-archive` so it's safe regardless.

## No-break verification per area

- **No-roster (#72):** dry-run a session with no roster on the deployed/test instance; confirm a self-entered candidate's score appears correctly in the admin Results tab; confirm no rows silently dropped.
- **Fullscreen (#70/#71):** browser E2E on :9222 — reload stub inside fullscreen and confirm fullscreen is NOT exited and no native dialog appears; toggle the red-screen mode live and confirm the typing step disappears while the warning + re-enter button remain.
- **Eval button (#73):** click the button on a contest with submissions; confirm verdicts/tags compute and re-running is safe.
- **Deploy (#76):** smoke the deployed web (200) + admin routes (401 unauth) after deploy; verify env preserved.

## Morning-discussion list (open judgment calls)

- **[RESOLVED 2026-06-17] PR #2 landing mechanism** — Karthi chose **B (merge, no force-push)**. Master keeps the old pre-rewrite lineage by design (duplicated subjects accepted).
- **[RESOLVED 2026-06-17] #72 selection scope** — Karthi: selection/shortlist won't be correct with no college/roster, and that's OK (no issue). Deliverable = scored **CSV export** + neutralize the alarming "unmatched / not on roster" banner → neutral "self-entered". NO in-panel selection build.
- **[RESOLVED 2026-06-17] #72 ID model** — uniqueness key = **roll number + contest id** (= username_norm within contest scope). A 2nd login on the same (roll, contest) is treated as the **SAME student re-logging-in** — handle GRACEFULLY via the existing duplicate-session / live-lock / resume path (2nd waits / resumes cleanly; scores attributed to the one identity; no corruption). NO new hard-reject guard. TASK = VERIFY this is graceful end-to-end for no-roster (esp. that a dropped student RESUMES on re-login rather than dead-ending in pending_approval) + polish the UX if confusing.
- **[OPS — brief invigilators] concurrent 2nd-device login** — a student re-logging in from a DIFFERENT device while their 1st session is still live waits in `pending_approval` until an admin approves/bypasses (Sessions tab). Normal same-device reconnects resume automatically. Rare for 700 students, but invigilators should know the approve flow.
- **[OPEN — optional] internal "keystroke" identifiers** — admin-panel + evidence strings are scrubbed (commit ba4e302), but internal var names/comments in evaluationMetrics.mjs (`n_keystrokes`, `METRONOMIC_MIN_KEYS`, code comments) still use the term — public-repo source but never rendered to admins. Optional deeper rename; low-priority/mechanical; deferred (not risking it on exam-eve).
- **[OPEN — needs Karthi, like PR #2] PII in git HISTORY** — a real student roll+score (the redacted `top=…` in RESUME-ANCHOR) still exists in git history (3 commits) AND is already on public origin/master (PRE-EXISTING leak; this branch's TIP is clean/redacted, so the merge brings a clean tip). gitleaks doesn't flag bare rolls. Remediation = history scrub via fresh-branch + git-filter-repo (Karthi's call on mechanism per the no-force-push-shared-master rule). NOT exam-blocking; do NOT autonomously rewrite history.
- **[OPS — brief invigilators] roll-collision detectability** — neutral no-roster copy removes the admin's signal to spot a merged/blocked identity from an ID-normalization collision. Brief invigilators: a "someone already logged in as you" on a fresh login = likely a roll typo/collision (student re-checks/retypes exact roll); admin should spot-check pending_approval sessions during the exam rather than rely on the now-neutral Results banner.
- **[OPEN — optional] force re-run timeout** — a force re-eval over ~700 candidates can hit the 120s Cloud Run request timeout (25/batch, sequential GCS gathers); recoverable by re-clicking (incremental skip resumes). If it stalls: lower EVALUATE_BATCH_LIMIT (env, e.g. 10) or raise the api `--timeout`.
- **[OPEN — optional] infra IDs in public night-run** — the plan doc + GCP-SETUP notes name the deployer SA email / archive bucket / Cloud Run URLs (NOT secrets). Consistent with the intentionally-public night-run, but optionally keep infra IDs private (c3 pattern). Low priority.
- **[deferred NITs]** stub-reload modal message could name the wrong language if the candidate switches language while it's open (cosmetic); ContestsPanel toggle resends the full enforcement object (dead fallback today, but could reset custom knobs if a contest ever loaded without enforcement).
- (add others as they arise — do not silently guess)

## Progress log (cont.)

- **2026-06-17 (~23:30 IST — Karthi → sleep; autonomous from here):** Browser VERIFIED (headless Chromium :9222 + MCP, self-managed). **#74 analysis DONE** — 4 reliable Tier-A signals (D7 recurring-pair, D5 hard-clone, D1 paste>0.6, D2 foreign-paste≥300); zero clean false-positives on KPR; existing `projectEvaluation` (scoreboard.mjs) already surfaces the right conservative shape → SHIP UNCHANGED. Only polish: grammar "a exact"/"a skeleton"→"an…" (evaluationMetrics.mjs:1448/1456) + optional D4 cadence wording (:548/556, never fired). Tier-C noise (D17 ip/fullscreen, D3 away-paste) correctly NOT surfaced — leave out. Ground-truth top-5 saved (junk/SELECTED-GROUND-TRUTH.md, gitignored) — ranks #3/#4 (two of the selected candidates) got only a Tier-B medium-clone WARNING → confirms Tier-B stays advisory, never a verdict. Stretch goal logged. Proceeding on #72 with safe defaults (CSV export + neutralize the "unmatched/not-on-roster" banner → "self-entered"; collision mitigation → recommend pre-issued IDs, flag for morning, no risky login-path change tonight). Workstream A (fullscreen #70+#71) still building.
- **2026-06-17 (ground-truth validated; #72 answered):** Ran the 5 selected (junk/SELECTED-GROUND-TRUTH.md) through KPR scorecards → ALL clean (integrity tier None, **0 critical flags, 0 false positives**), talent composite 45–68 (rank-1 = 68). #3/#4 (two of the selected candidates) carry only Tier-B warnings → confirms Tier-B stays advisory. Validates: ship `projectEvaluation` as-is; keep warnings non-verdict. Karthi answered both #72 calls (see morning list, now RESOLVED): CSV-only selection (OK to lose shortlist); ID model = roll+contest unique, duplicate = same-user graceful re-login (verify, don't add a hard guard).

## Progress log

- **2026-06-17:** Recon sweep launched (read-only, 6 areas, COMPLETE). PR #2 diagnosed + verified clean & superset. Tasks #69–#77 created. KPR test data preserved earlier (evidence-bucket lifecycle cleared + copied to `aerele-proctor-dev-kpr-archive`).
- **2026-06-17 (step 1 DONE):** PR #2 MERGED (Option B, no force-push) → `master = e187858`, tree byte-identical to the clean branch, 0 verdict-queue, full history PII-clean (gitleaks). Local synced; working branch `fix/exam-eve-2026-06-18` cut off clean master. Goal set. Recon results captured.
- **2026-06-17 (recon ingested; builds dispatched):** KEY FINDINGS — (i) **#73 eval button ALREADY EXISTS** (on-demand "Evaluate contest" on Results page, idempotent, force path wired) → reduces to rename + a visible force re-run toggle. (ii) **#72 no-roster: scores compute CORRECTLY** (keyed on typed id / username_norm), BUT all rows show as "unmatched identity" with an alarming "not on roster" banner + selection disabled (reframe needed), AND a REAL hazard: two students whose typed ids normalize to the same string collide → scores merge + 2nd blocked mid-exam (needs mitigation). No automated test for the zero-enrollment Results path. (iii) **#74 pattern analysis already written** (junk/KPR-ROUND2-ANALYSIS.md + junk/out/scorecards). (iv) the no-roster fix set is commit `0844560` (the cited `89e20a1` doesn't exist). DISPATCHED (subagents): Workstream A = fullscreen #70+#71 build (TDD, uncommitted); #74 = read-only KPR pattern analysis. AWAITING Karthi on 2 no-roster judgment calls (selection scope; collision mitigation). Workstream B (results: #72 reframe + #73 rename/force + #74 surfacing + #75 wording) sequenced AFTER A is verified+committed.
