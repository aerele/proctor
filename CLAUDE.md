# CLAUDE.md — Proctor

All instructions are in `AGENTS.md`. **Read it now** before doing anything else —
it covers what Proctor is, where to read, session-start steps, and (critically)
**how the roadmap + backlog are maintained** so requests never get silently dropped.
